    <p id="notice"></p>
  <div class="container px-5 px-sm-0">
    <div class="row">
     <div class="col-md-3">
      <%= render 'part', user: @user %>
      <!-- 部分テンプレート -->
     </div>

      <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <table class="table ">
　      <tr>
　       <th>Title</th><th>Opinion</th><th></th></tr>
　     <% @user.books.each do |book| %>
　      <tr>
　        <td><%= book.title %></td>
　        <td><%= book.body %></td>
　        <td></td>
　      </tr>
　     <% end %>
      </table>
     </div>
     </div>
  　</div>
