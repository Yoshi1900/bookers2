    <p id="notice"></p>
    <div class="container px-5 px-sm-0">
      <div class="row">
        <div class="col-md-3">

          <%= render 'user/part', user: @user %>
           <!-- 部分テンプレート -->
        </div>
        <div class="col-md-8 offset-md-1">
         <h2><strong>Book detail</strong></h2>
          <table class="table table-hover table-inverse">
           <tr>
            <td></td>
            <td><%= image_tag @user.get_profile_image(100,100) %></td>
            <td><%= link_to book_path(@user) %><%= book.title %></td>
            <td><%= @book.body %></td>
            <td><%= link_to edit_book_path class: 'btn btn-sm btn-success' do %>Edit<% end %></td>
            <td><%= link_to 'Destroy', book, method: :delete, data: {confirm: "本当に消しますか？"},class: 'btm' %></td>
            <% end %>
           </tr>
          </table>
       </div>
     </div>
   </div>